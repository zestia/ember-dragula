<div class="ember-dragula" ...attributes {{this.dragula}}>
  {{#let
    (component
      this.EmberDragulaContainer
      onInsert=this.addContainer
      onDestroy=this.removeContainer
    )
    as |Container|
  }}
    {{yield (hash Container=Container)}}
  {{/let}}
</div>