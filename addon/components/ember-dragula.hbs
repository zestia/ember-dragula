<div class="ember-dragula" ...attributes {{this.handleDestroyElement}}>
  {{yield
    (hash
      Container=(component
        "ember-dragula-container"
        onInsert=this.addContainer
        onDestroy=this.removeContainer
      )
    )
  }}
</div>