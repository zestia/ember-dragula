<div class="ember-dragula" {{will-destroy this.handleDestroyElement}} ...attributes>
  {{yield
    (hash
      Container=(component
        "ember-dragula-container"
        onInsert=this.addContainer
        onDestroy=this.removeContainer
      )
    )
  }}
</div>