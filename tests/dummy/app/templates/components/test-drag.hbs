{{#dragulember-sortable}}
  {{#dragulember-sortable-container items=list }}
    {{#each list as |item|}}
        <div id={{item.name}}>{{item.name}}</div>
    {{/each}}
  {{/dragulember-sortable-container}}

  {{#dragulember-sortable-container items=listTwo }}
    {{#each listTwo as |item|}}
        <div id={{item.name}}>{{item.name}}</div>
    {{/each}}
  {{/dragulember-sortable-container}}
{{/dragulember-sortable}}
